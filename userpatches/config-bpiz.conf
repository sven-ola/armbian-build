# ./compile.sh CPUTHREADS=$(nproc) bpiz

BOARD=bananapim2zero
BRANCH=current
RELEASE=trixie

KERNEL_CONFIGURE=yes
BUILD_DESKTOP=no
BUILD_MINIMAL=yes

PREFER_DOCKER=yes
KERNEL_GIT=shallow
SKIP_ARMBIAN_REPO=yes
