# ./compile.sh CPUTHREADS=$(nproc) x86

BOARD=uefi-x86
BRANCH=current
RELEASE=trixie

KERNEL_CONFIGURE=no
ARTIFACT_IGNORE_CACHE=yes
BUILD_DESKTOP=no
BUILD_MINIMAL=yes

PREFER_DOCKER=yes
KERNEL_GIT=shallow
SKIP_ARMBIAN_REPO=yes
